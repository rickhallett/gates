---
version: 1.0
runtime: docker

build:
  dockerfile: Dockerfile

run:
  runtime-version: 1.0
  network:
    port: 80
  env:
    - name: PYTHONUNBUFFERED
      value: "1"
    - name: ENVIRONMENT
      value: "production"
